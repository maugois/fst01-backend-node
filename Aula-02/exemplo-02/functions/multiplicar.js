export const multiplicar = (firtsNumber, secondNumber) => {
    return Number(firtsNumber) * Number(secondNumber)
}

export function potencia (firtsNumber, secondNumber) {
    return Number(firtsNumber) ** Number(secondNumber)
}