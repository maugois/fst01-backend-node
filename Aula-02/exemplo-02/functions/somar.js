const somar = (firtsNumber, secondNumber) => {
    return Number(firtsNumber) + Number(secondNumber)
}

const somar2 = (firtsNumber, secondNumber) => {
    return Number(firtsNumber) + Number(secondNumber)
}

export default { somar, somar2 }